import React, { useEffect } from 'react';
import PostGrid from '../../containers/postGrid';

const Home = () => {
  useEffect(() => {
  }, []);

  return (
    <PostGrid />
  );
};

export default Home;
